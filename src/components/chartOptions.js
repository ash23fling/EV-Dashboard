export const barChartOptions = {
  responsive: true,
  maintainAspectRatio: true,
  aspectRatio: 1.3, 
  plugins: {
    legend: {
      position: "top",
      align: "center",
    },
  },
};

export const pieChartOptions = {
  responsive: true,
  maintainAspectRatio: true,
  aspectRatio: 1.3, 
  plugins: {
    legend: {
      position: "top",
      align: "center",
    },
  },
};
